Select to all between any bracket.
Shif+v
f + ({[
% + ]})

When carriage in ({[ 
press % to the end ]})

Delete inside {} () []
d+i+ [{(

Delete with bracket.
d+a+ [{(

Delete and start write inside bracket || delete with bracket and write, replace i to a.
c+i+ [{(

Yank, Delete, replace some in "", '', ``.
Pres in line c+i+"", '', ``
